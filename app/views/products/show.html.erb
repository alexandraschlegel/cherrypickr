
<div class="product-container">
  <div class="left-section">
    <h1> Your Product in Your Area
    <%= image_tag @product.image, alt: "alttext" %>
    <h2><%= @product.name %></h2>

    <div class="d-flex">
      <%= @product.supermarket.name %>
      <%= @product.price %>
      <% @other_ones.each do |f| %>
      <p><%= f.supermarket.name %></p>
      <p><%= f.price %></p>
      <% end %>
    </div>
<!-- Button trigger modal -->
    <button type="button" class="btn" data-toggle="modal" data-target="#exampleModalLong">
      Get Price alert
    </button>
    <!-- Modal -->
    <div class="modal fade" id="exampleModalLong" tabindex="-1" role="dialog" aria-labelledby="exampleModalLongTitle" aria-hidden="true">
      <div class="modal-dialog" role="document">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="exampleModalLongTitle">Modal title</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">&times;</span>
            </button>
          </div>
          <div class="modal-body">
            ...
          </div>
          <div class="modal-footer">
            <button type="button" class="btn" data-dismiss="modal">Search for more</button>
        </div>
        </div>
      </div>
    </div>
  </div>
  <div class="right section">
    <div class="top sub-section">
    </div>
  </div>
</div>
<!--
PSEUDO CODE

1) Make a list based on search term with products with exact same name
2) it should be in form of a hash
3) we want to get the value for the key price and key supermarket
4) display those

search_results = []

def search
  if x= product.name == query.search
  search_results << x
  else
  return "product not found"


end
 -->





